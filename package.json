{
  "name": "movie-business-server",
  "version": "3.0.0",
  "private": true,
  "description": "Movie Business 3 save/load backend (PostgreSQL persistent storage on Render)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "pg": "^8.13.0"
  },
  "engines": {
    "node": ">=18"
  }
}
